{
  "title": "Build Image Schema",
  "id": "BuildImageSchema",
  "description": "Build Image options",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "default": ""
    },
    "buildCommand": {
      "type": "string",
      "description": "The command will be used to build an application.",
      "default": "build"
    },
    "verbose": {
      "type": "boolean",
      "description": "Whether to output all docker logs or not.",
      "default": false
    }
  }
}
