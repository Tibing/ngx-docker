{
  "title": "Run Schema",
  "id": "RunSchema",
  "description": "Run options",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "default": ""
    },
    "port": {
      "type": "number",
      "description": "Port to start container on.",
      "default": 4200
    }
  }
}
